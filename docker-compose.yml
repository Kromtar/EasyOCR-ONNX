version: "3.7"

services:
  easyocr:
    build:
      context: ./
      dockerfile: Dockerfile_onnx
    container_name: easyocr
    volumes:
      - "./:/app"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]